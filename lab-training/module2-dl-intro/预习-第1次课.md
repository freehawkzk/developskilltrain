# 预习文档：模块2 第1次课 —— 从感知机到神经网络

> 请在上课前阅读本文档。

## 你应该知道的概念

| 术语 | 含义 | 你可以类比为 |
|------|------|-------------|
| 损失函数 | 衡量预测和真实的差距 | 测量误差 |
| 梯度 | 损失函数对参数的偏导数 | 告诉你"该变大还是变小" |
| 梯度下降 | 沿梯度反方向更新参数 | 下山——走最陡的下坡路 |
| 学习率 | 每次参数更新的步长 | 下山的步伐大小 |
| 张量(Tensor) | 多维数组 | NumPy数组 + GPU加速 |

## 思考题

1. 如果你要让计算机学会区分"低频声音"和"高频声音"，你会怎么定义"学会"的标准？
2. 梯度下降中，学习率太大或太小分别会怎样？

## 环境检查

- [ ] `import torch` 不报错
- [ ] `torch.cuda.is_available()` 返回 True（如有GPU）
- [ ] `import numpy as np` 不报错
- [ ] `import matplotlib.pyplot as plt` 不报错
